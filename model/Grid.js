class Grid {
    constructor(idGrid,tipo,userId,isComplete){
        this.idGrid=idGrid;
        this.tipo=tipo;
        this.userId=userId;
        this.isComplete = isComplete;
    }
}
export default Grid;